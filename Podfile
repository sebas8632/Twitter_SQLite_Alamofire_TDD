# Uncomment the next line to define a global platform for your project
 platform :ios, '11.2'

target 'TwitterApp-sqlite-alamofire-TDD' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TwitterApp-sqlite-alamofire-TDD
	pod 'SQLite.swift', '~> 0.11.4'
        pod 'SwiftyJSON'
        pod 'Alamofire', '~> 4.7'
        pod 'AlamofireImage', '~> 3.3'

#        pod 'RxSwift',    '~> 4.0'
#        pod 'RxCocoa',    '~> 4.0'

  target 'TwitterApp-sqlite-alamofire-TDDTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TwitterApp-sqlite-alamofire-TDDUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
